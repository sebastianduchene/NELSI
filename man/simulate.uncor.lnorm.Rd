% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.uncor.lnorm.R
\name{simulate.uncor.lnorm}
\alias{simulate.uncor.lnorm}
\title{Simulate uncorrelated lognormal rates}
\usage{
\method{simulate}{uncor.lnorm}(tree, params = list(mean.log = -5, sd.log = 0.5))
}
\arguments{
\item{tree}{A rooted chronogram of class \code{"phylo"} with branch lengths
in units of time.}

\item{params}{A list with elements:
\describe{
  \item{\code{mean.log}}{Numeric. Mean of the lognormal distribution on
    the log scale. Default \code{-5}.}
  \item{\code{sd.log}}{Numeric. Standard deviation on the log scale.
    Default \code{0.5}.}
}}
}
\value{
An object of class \code{"ratesim"}; see \code{\link{simulate.rate}}.
}
\description{
Simulates evolutionary rates along a phylogenetic tree under the
uncorrelated lognormal (UCLN) relaxed-clock model. Each branch rate is
drawn independently from a lognormal distribution.
}
\examples{
library(ape)
set.seed(1)
tr <- rcoal(10)
sim <- simulate.rate(tr, simulate.uncor.lnorm,
                     list(mean.log = -5, sd.log = 0.5))
plot(sim)

}
\seealso{
\code{\link{simulate.rate}}, \code{\link{simulate.uncor.gamma}},
  \code{\link{simulate.uncor.exp}}
}
