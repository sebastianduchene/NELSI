% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotatedTreeReader.R
\name{read.annotated.tree}
\alias{read.annotated.tree}
\title{Read Newick trees with BEAST-style annotations}
\usage{
read.annotated.tree(
  file = "",
  text = NULL,
  tree.names = NULL,
  skip = 0,
  comment.char = "#",
  keep.multi = FALSE,
  ...
)
}
\arguments{
\item{file}{Character. Path to the Newick file. Ignored if \code{text} is
supplied. Default \code{""} (reads from standard input).}

\item{text}{Character vector of Newick strings, one per tree. If
non-\code{NULL}, overrides \code{file}.}

\item{tree.names}{Character vector of tree names. Default \code{NULL}.}

\item{skip}{Integer. Number of lines to skip at the start of \code{file}.
Default \code{0}.}

\item{comment.char}{Character. Lines beginning with this character are
ignored. Default \code{"#"}.}

\item{keep.multi}{Logical. If \code{TRUE}, a single tree is still returned
as a \code{"multiPhylo"} list. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[base]{scan}}.}
}
\value{
A \code{"phylo"} object (or \code{"multiPhylo"} for multiple trees)
  with an additional \code{$annotations} list element containing the parsed
  BEAST annotations for each branch.
}
\description{
Reads one or more Newick-format trees that contain BEAST-style square-
bracket annotations (e.g. \code{[&rate_median=0.01,length=0.5]}). The
annotations are parsed and stored in the \code{$annotations} list of the
returned \code{"phylo"} object. Largely derived from \code{ape::read.tree}.
}
\examples{
\dontrun{
tr <- read.annotated.tree("beast_mcc.tree")
names(tr$annotations[[1]])
}

}
\seealso{
\code{\link{read.annotated.nexus}}, \code{\link{trann2trdat}}
}
