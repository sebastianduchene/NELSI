% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allnode.times.R
\name{allnode.times}
\alias{allnode.times}
\title{Ages of all nodes in a phylogenetic tree}
\usage{
allnode.times(phylo, tipsonly = FALSE, reverse = TRUE, keeproot = FALSE)
}
\arguments{
\item{phylo}{A rooted phylogenetic tree of class \code{"phylo"} with branch
lengths in units of time.}

\item{tipsonly}{Logical. If \code{TRUE}, returns ages of tips only. Default
\code{FALSE} returns ages of all nodes.}

\item{reverse}{Logical. If \code{TRUE} (default), ages are expressed as time
before the most recent tip (i.e. the youngest tip has age 0). If
\code{FALSE}, the root has the maximum value.}

\item{keeproot}{Logical. If \code{TRUE} and the tree has a \code{root.edge},
its length is added to all node ages. Default \code{FALSE}.}
}
\value{
A named numeric vector of node ages. Names are the integer node
  indices following ape convention (tips \code{1..Ntips}, internal nodes
  \code{Ntips+1..Ntips+Nnodes}).
}
\description{
Returns the ages (divergence times) of all nodes — tips and internal nodes —
in a phylogenetic tree. Useful for analysing heterochronous trees where tip
ages vary.
}
\examples{
library(ape)
set.seed(1)
tr <- rcoal(10)
allnode.times(tr)
allnode.times(tr, tipsonly = TRUE)

}
\seealso{
\code{\link{intnode.times}}, \code{\link{get.rtt.dist}}
}
