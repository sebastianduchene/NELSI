% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.tree.lines.R
\name{plot.tree.lines}
\alias{plot.tree.lines}
\title{Plot a phylogenetic tree using line segments}
\usage{
\method{plot}{tree.lines}(
  tree,
  rotation.angle = 0,
  x.offset = 0,
  y.offset = 0,
  log.scale = F,
  line.type = "s",
  plot.new = F,
  show.tip.labels = F,
  lines.colour = "darkgrey",
  tips.colour = "black",
  branch.width = 2,
  ...
)
}
\arguments{
\item{tree}{A rooted phylogenetic tree of class \code{"phylo"}.}

\item{rotation.angle}{Numeric. Angle in radians by which to rotate the
entire plot. Default \code{0}.}

\item{x.offset}{Numeric. Horizontal shift applied after rotation. Default
\code{0}.}

\item{y.offset}{Numeric. Vertical shift. If \code{0} (default) the minimum
y coordinate is set to 0.}

\item{log.scale}{Logical. If \code{TRUE}, y coordinates are
\code{log10(1 + y)}. Default \code{FALSE}.}

\item{line.type}{Character. Line type passed to \code{\link[graphics]{lines}}.
Default \code{"s"} (staircase).}

\item{plot.new}{Logical. If \code{TRUE}, a new blank plot is opened before
drawing. Default \code{FALSE}.}

\item{show.tip.labels}{Logical. If \code{TRUE}, tip labels are printed.
Default \code{FALSE}.}

\item{lines.colour}{Character vector. Colours for each branch (recycled to
match number of edges). Default \code{"darkgrey"}.}

\item{tips.colour}{Character. Colour for tip points. Default \code{"black"}.}

\item{branch.width}{Numeric. Line width. Default \code{2}.}

\item{...}{Additional arguments passed to \code{\link[graphics]{plot}} when
\code{plot.new = TRUE}.}
}
\value{
\code{NULL} invisibly. Called for its plotting side effect.
}
\description{
Draws a phylogenetic tree as a series of line segments with optional
rotation, offset, and log-scale y axis. Unlike standard \code{plot.phylo},
this function uses internally computed node coordinates
(\code{\link{get.ordinates}}) and supports per-branch colouring.
}
\examples{
library(ape)
set.seed(1)
tr <- rcoal(10)
pdf(NULL)
plot.tree.lines(tr, plot.new = TRUE)
dev.off()

}
\seealso{
\code{\link{get.ordinates}}
}
