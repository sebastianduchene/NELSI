% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.uncor.gamma.R
\name{simulate.uncor.gamma}
\alias{simulate.uncor.gamma}
\title{Simulate uncorrelated gamma-distributed rates}
\usage{
\method{simulate}{uncor.gamma}(tree, params = list(mean = 0.007, shape = 3.98, rate = NULL))
}
\arguments{
\item{tree}{A rooted chronogram of class \code{"phylo"} with branch lengths
in units of time.}

\item{params}{A list with elements:
\describe{
  \item{\code{mean}}{Numeric. Mean of the gamma distribution. Default
    \code{0.007}.}
  \item{\code{shape}}{Numeric. Shape parameter. Default \code{3.98}.}
  \item{\code{rate}}{Numeric or \code{NULL}. Rate parameter. If
    \code{NULL} (default), computed as \code{shape / mean}.}
}}
}
\value{
An object of class \code{"ratesim"}; see \code{\link{simulate.rate}}.
}
\description{
Simulates evolutionary rates along a phylogenetic tree under an uncorrelated
relaxed-clock model where each branch rate is drawn independently from a
gamma distribution. If \code{rate} is \code{NULL} it is inferred as
\code{shape / mean}.
}
\examples{
library(ape)
set.seed(1)
tr <- rcoal(10)
sim <- simulate.rate(tr, simulate.uncor.gamma,
                     list(mean = 0.007, shape = 3.98))
plot(sim)

}
\seealso{
\code{\link{simulate.rate}}, \code{\link{simulate.uncor.lnorm}},
  \code{\link{simulate.uncor.exp}}
}
