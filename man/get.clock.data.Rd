% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.clock.data.R
\name{get.clock.data}
\alias{get.clock.data}
\title{Extract and plot root-to-tip distances vs sampling times}
\usage{
get.clock.data(rate.sim.object, tipsonly = T, ...)
}
\arguments{
\item{rate.sim.object}{An object of class \code{"ratesim"} as returned by
\code{\link{simulate.rate}}.}

\item{tipsonly}{Logical. If \code{TRUE} (default), returns values for tips
only.}

\item{...}{Additional arguments passed to \code{\link[graphics]{plot}}.}
}
\value{
A \code{data.frame} with columns \code{times} (root-to-tip
  distances in time units) and \code{substitutions} (root-to-tip distances
  in substitution units). A scatter plot is produced as a side effect.
}
\description{
From a rate-simulation object, extracts root-to-tip distances in
substitution units and in time units, produces a scatter plot, and returns
both as a data frame. Useful for visualising the clock signal.
}
\examples{
library(ape)
set.seed(1)
tr <- rcoal(10)
sim <- simulate.rate(tr, simulate.uncor.lnorm,
                     list(mean.log = -5, sd.log = 0.5))
pdf(NULL)
get.clock.data(sim)
dev.off()

}
\seealso{
\code{\link{simulate.rate}}, \code{\link{allnode.times}}
}
