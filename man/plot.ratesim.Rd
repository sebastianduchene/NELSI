% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.ratesim.R
\name{plot.ratesim}
\alias{plot.ratesim}
\title{Plot a rate simulation object}
\usage{
\method{plot}{ratesim}(x, col.lineages = colors(), type = "l")
}
\arguments{
\item{x}{An object of class \code{"ratesim"} as returned by
\code{\link{simulate.rate}}.}

\item{col.lineages}{Character vector of colours, one per tip lineage.
Default uses \code{\link[grDevices]{colors}()}.}

\item{type}{Character. Line type passed to \code{\link[graphics]{plot}}.
Default \code{"l"}.}
}
\value{
\code{NULL} invisibly. The function is called for its plotting side
  effect.
}
\description{
S3 plot method for objects of class \code{"ratesim"}. Produces a two-panel
figure: a rate-through-time plot (one line per tip lineage) and a
phylogram with branch widths proportional to the log rate.
}
\examples{
library(ape)
set.seed(1)
tr <- rcoal(10)
sim <- simulate.rate(tr, simulate.clock, list(rate = 0.005, noise = 1e-6))
pdf(NULL)
plot(sim)
dev.off()

}
\seealso{
\code{\link{simulate.rate}}, \code{\link{get.lineage.time.rate}}
}
