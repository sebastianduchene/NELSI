% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.descending.nodes.branches.R
\name{get.descending.nodes.branches}
\alias{get.descending.nodes.branches}
\title{Descendant nodes and branches of a subtree}
\usage{
get.descending.nodes.branches(tr, target_node)
}
\arguments{
\item{tr}{A rooted phylogenetic tree of class \code{"phylo"}.}

\item{target_node}{Integer node index (ape convention). Passing a tip index
returns that tip only with no descendant branches.}
}
\value{
A named list with two elements:
  \describe{
    \item{\code{descending.nodes}}{Integer vector of all node indices in the
      subtree (tips and internal nodes), including \code{target_node}.}
    \item{\code{descending.branches}}{Integer vector of edge row-indices in
      \code{tr$edge} for every edge within the subtree.}
  }
}
\description{
Returns all node indices and edge indices within the subtree rooted at
\code{target_node}, including the target node itself.
}
\examples{
library(ape)
set.seed(1)
tr <- rcoal(10)
root_node <- length(tr$tip.label) + 1L
get.descending.nodes.branches(tr, root_node)

}
\seealso{
\code{\link{get.ancestor.nodes.branches}}, \code{\link{sum.descending.branches}}
}
