% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.white.noise.R
\name{simulate.white.noise}
\alias{simulate.white.noise}
\title{Simulate white-noise rates}
\usage{
\method{simulate}{white.noise}(tree, params = list(rate = 0.006, var = 1e-06))
}
\arguments{
\item{tree}{A rooted chronogram of class \code{"phylo"} with branch lengths
in units of time.}

\item{params}{A list with elements:
\describe{
  \item{\code{rate}}{Numeric. The base substitution rate. Default
    \code{0.006}.}
  \item{\code{var}}{Numeric. Variance scaling parameter for the lognormal
    noise. Default \code{1e-6}.}
}}
}
\value{
An object of class \code{"ratesim"}; see \code{\link{simulate.rate}}.
}
\description{
Simulates evolutionary rates along a phylogenetic tree under a white-noise
model. Each branch's substitution length is drawn from a lognormal
distribution centred on the strict-clock expectation (\code{rate *
branch.length}), with variance scaled by the branch's standardised
substitution length.
}
\examples{
library(ape)
set.seed(1)
tr <- rcoal(10)
sim <- simulate.rate(tr, simulate.white.noise,
                     list(rate = 0.006, var = 1e-6))
plot(sim)

}
\seealso{
\code{\link{simulate.rate}}, \code{\link{simulate.clock}}
}
